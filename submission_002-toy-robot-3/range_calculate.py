calcu = input("prompt")

    elif command_name == 'replay':
        list
        if len(arg) == 3:
            n = int(arg[0])
            m = int(arg[2])
        elif len(arg) == 1:
            n = int(arg[0])
            m = 0
        else:
            n = len(history) - 1
            m = 0
        (do_next, command_output) = do_replay(robot_name, n, m)

print(calcu)